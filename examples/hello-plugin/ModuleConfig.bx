/**
 * Minimal module descriptor - a bx-docs plugin needs nothing beyond what
 * every BoxLang module already needs: this file (so it registers/
 * activates), and models/BxDocsPlugin.bx (so bx-docs finds its hooks -
 * see docs/guides/plugins.md).
 */
class {

	this.author  = "Ortus Solutions";
	this.version = "1.0.0";

	function configure() {
		settings = {};
	}

	function onLoad() {
	}

}
